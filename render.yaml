services:
  - type: web
    name: wechat-article-to-pdf
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    # Render 会自动检测端口
